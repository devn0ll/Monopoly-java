public class Field {
	boolean GameEnd = false;
	//turn
    public void turn () {
        //for each Player move-phase,buy/pay-phase,trade-phase
    }
    //constructor
    Field (int count) {
        if (count==1){
            Player pl1 = new Player(1);
        }
    }
}
